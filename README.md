# clap-mml-play-server

- Purpose:
  - Intended for use as a library by `clap-mml-render-tui`.

- Background:
  - This project was cloned from the original repository (`clap-mml-render-tui`) and spun off. It retains the commit history up to the point of the spin-off.

- Memo:
  - The actual server, CLI, and TUI functionalities are implemented within `clap-mml-render-tui`.